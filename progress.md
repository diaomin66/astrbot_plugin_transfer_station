## 2026-07-29 - Task: 实现 AstrBot 中转站新人礼插件并准备 GitHub 发布

### What was done

- 完成 QQ OneBot/NapCat 新人入群资格记录、可配置欢迎消息和精确口令识别。
- 完成全局一次领取、群临时会话发码、成功删除兑换码及失败事务回滚。
- 完成 SQLite 持久化、并发串行化和不保留已领取完整兑换码的隐私处理。
- 完成 AstrBot Plugin Page 兑换码批量导入、库存分页、遮罩显示、复制、删除、统计和领取记录。
- 完成插件配置、元数据、中英文 Page 标题、安装使用文档和自动化测试。

### Testing

- `python -m pytest -q`：14 项测试全部通过。
- `python -m ruff check .`：通过。
- `python -m ruff format --check .`：通过。
- `python -m py_compile main.py page_api.py storage.py`：通过。
- `node --check pages/gift_codes/app.js`：通过。
- AstrBot 4.26.7 非侵入式兼容验证：元数据加载、插件类识别和 `gift_codes` Page 发现均通过。
- 未执行真实 QQ 群临时会话端到端发放；该项需要在线 NapCat 账号、测试群和实际新人账号。

### Notes

- `main.py`：新增入群欢迎、资格判断、精确领取口令和 NapCat 群临时会话发码流程。
- `storage.py`：新增 SQLite 初始化、库存、新人资格、领取记录、并发锁和事务回滚。
- `page_api.py`：新增 Plugin Page 统计、库存、导入、删除和领取记录 API。
- `pages/gift_codes/index.html`：新增新人礼管理页面结构。
- `pages/gift_codes/style.css`：新增响应式明暗主题管理页面样式。
- `pages/gift_codes/app.js`：新增 Page Bridge 数据读取、导入、遮罩、复制、删除和分页交互。
- `metadata.yaml`：新增插件身份、版本、平台兼容和 Page 元数据。
- `_conf_schema.json`：新增总开关、群白名单、欢迎内容、领取口令和新人提醒配置。
- `.astrbot-plugin/i18n/zh-CN.json`：新增中文插件与 Page 标题。
- `.astrbot-plugin/i18n/en-US.json`：新增英文插件与 Page 标题。
- `requirements.txt`：声明 SQLite 异步驱动依赖。
- `README.md`：新增功能、安装、使用、限制和验证说明。
- `docs/USAGE.md`：新增配置、运营、备份、临时会话要求和故障排查文档。
- `tests/conftest.py`：新增仓库目录无关的插件模块加载辅助。
- `tests/test_storage.py`：新增存储、回滚、全局一次领取和并发库存测试。
- `tests/test_plugin.py`：新增入群、白名单、口令、发码和失败回滚测试。
- `tests/test_page_api.py`：新增 Page API 导入、分页、删除和参数验证测试。
- `tests/test_static_contract.py`：新增元数据、配置和前端安全渲染契约测试。
- `.gitignore`：新增 Python、测试、IDE 和运行时数据库忽略规则。
- `LICENSE`：新增 MIT 许可证。
- `progress.md`：追加本轮实现、验证、文件清单和回滚说明。
- 回滚方式：发布后在仓库执行 `git revert <本次初始提交哈希>`；若已安装到 AstrBot，可同时删除 `data/plugins/astrbot_plugin_transfer_station`，需要清除业务数据时再手动删除 `data/plugin_data/astrbot_plugin_transfer_station`。

## 2026-07-29 - Task: 发布公开 GitHub 仓库

### What was done

- 将本地默认分支从 `master` 调整为 `main`。
- 创建公开仓库 `diaomin66/astrbot_plugin_transfer_station`。
- 将初始功能提交 `2a8ef395aaf2e1595e5ba8961da8b270b776e938` 推送到远端 `main`。

### Testing

- `gh repo view` 确认仓库可访问、可见性为 `PUBLIC`、默认分支为 `main`。
- `git ls-remote --heads origin main` 确认远端 `main` 指向初始功能提交。
- GitHub Commits API 确认远端提交信息为 `feat: add transfer station newcomer gift plugin`。

### Notes

- `progress.md`：追加 GitHub 创建、推送和远端核验结果。
- 回滚点：功能代码提交为 `2a8ef395aaf2e1595e5ba8961da8b270b776e938`；如需回退功能，执行 `git revert 2a8ef395aaf2e1595e5ba8961da8b270b776e938` 后推送。删除公开仓库属于高风险操作，应由仓库所有者在 GitHub 设置中单独确认。

## 2026-07-29 - Task: 修复兑换码列表错位和删除无响应

### What was done

- 修复兑换码 `<td>` 使用 Flex 布局导致表头和数据列错位的问题，改为固定表格布局和明确列宽。
- 移除 AstrBot 沙箱 iframe 不支持的原生 `window.confirm()`，改为按钮内两次点击确认删除。
- 增加删除确认状态和自动复位反馈，第二次点击后调用现有 Page Bridge 删除接口。
- 将插件版本从 `v1.0.0` 提升到 `v1.0.1`。

### Testing

- Playwright 使用与 AstrBot 一致的 `sandbox="allow-scripts allow-forms allow-downloads"` iframe 复现：修复前删除接口调用次数为 0，并出现 `confirm()` 被沙箱拦截警告。
- 修复后 Playwright 验证四列表头和数据单元格位置、宽度一致，所有兑换码数据单元格保持 `display: table-cell`。
- 修复后首次点击进入确认状态且不调用接口，第二次点击调用删除接口 1 次，沙箱警告为 0。
- `python -m pytest -q`：14 项测试全部通过。
- `python -m ruff check .`、`python -m ruff format --check .`、Python 编译和 JavaScript 语法检查全部通过。

### Notes

- `pages/gift_codes/index.html`：为兑换码库存表增加专用表格类。
- `pages/gift_codes/style.css`：修复表格单元格布局并增加确认删除状态样式。
- `pages/gift_codes/app.js`：用沙箱兼容的按钮二次确认替换原生确认弹窗。
- `tests/test_static_contract.py`：增加表格布局、禁用 `window.confirm()` 和确认删除逻辑契约。
- `main.py`：同步插件内部版本为 `1.0.1`。
- `metadata.yaml`：发布版本提升为 `v1.0.1`。
- `progress.md`：追加本轮根因、修复和验证记录。
- 回滚方式：执行 `git revert <本轮修复提交哈希>` 后推送；回滚后将恢复原表格布局和原生确认弹窗行为。

## 2026-07-29 - Task: 增加临时会话失败引导和全部机器人文案自定义

### What was done

- 主动发起 QQ 群临时会话失败时，默认引导新人先主动私聊机器人发送任意消息建立会话，再回原群重新 `@机器人` 并发送领取口令。
- 保持失败时兑换码回库且不写入领取记录；用户完成会话建立后可直接重新领取，成功后才消耗兑换码。
- 将欢迎、兑换码私聊、发送成功、重复领取、无资格、库存不足、临时会话失败和通用失败文案全部加入插件配置。
- 私聊兑换码文案支持 `{code}` 和 `{claim_phrase}`；若管理员遗漏 `{code}`，插件会自动在文案末尾附加兑换码，避免成功领取却未收到兑换码。
- 插件版本升级为 `v1.1.0`，并同步更新使用与故障排查说明。

### Testing

- 使用本机 AstrBot `4.26.7` 实例的 Python `3.12.13` 环境执行 `pytest -q`：`16 passed`。
- 新增回归验证覆盖全部机器人聊天文案自定义、兑换码占位符保护、首次临时会话失败回库和再次领取成功。
- `ruff check .`：通过。
- `ruff format --check .`：通过，`11 files already formatted`。
- `py_compile main.py page_api.py storage.py`：通过。
- `node --check pages/gift_codes/app.js`：通过。
- `metadata.yaml` 与 `_conf_schema.json` 解析及版本/配置契约检查：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示。
- 未执行真实 QQ/NapCat 在线端到端发送；最终效果仍需在实际测试群中完成一次“首次失败、主动私聊、回群重试”的人工验收。

### Notes

- `main.py`：接入全部聊天文案配置、失败引导和可重试发送流程。
- `_conf_schema.json`：新增兑换码私聊及各类领取结果的自定义文案配置。
- `metadata.yaml`：版本升级为 `v1.1.0`。
- `README.md`：补充全部文案可配置及主动私聊后重试的使用说明。
- `docs/USAGE.md`：补充配置项、临时会话失败恢复步骤和排障结论。
- `tests/test_plugin.py`：新增文案自定义、兑换码占位符和失败后重试测试。
- `tests/test_static_contract.py`：更新版本与配置项契约。
- `progress.md`：追加本轮实施、验证和回滚记录。
- 无数据库结构变更，无需迁移现有资格、库存或领取记录。
- 回滚点：`af7d833`；发布后如需撤销本轮功能，可对本次功能提交执行 `git revert <本次提交哈希>`。

## 2026-07-29 - Task: 建立永久用户基线并阻止退群重进刷取

### What was done

- 将数据库升级到 schema v2，新增 QQ ID 全局唯一的永久用户库和群成员基线表。
- 插件每次启动都会通过 OneBot 同步白名单群当前全部成员；白名单为空时先读取机器人所在全部群，再逐群同步。
- 旧版新人资格和领取记录自动迁移为永久已知用户，升级时已经在群内的成员不会获得新人资格。
- 只有永久用户库中从未出现过的 QQ ID，才能在基线完成后的首次入群事件中获得一次资格。
- 已记录用户退群重进、跨群再次加入或重复入群通知均不会新增资格；成功发码后同时保留全局领取记录并清空该用户可领取群。
- 启动同步失败时采用失败关闭，停止该群发放并回复可自定义的成员基线未完成提示；后台每 60 秒重试。
- Page 新增数据库人数和今日新人统计，按服务器本地日期计算并每 10 秒自动刷新。
- 插件版本升级为 `v1.2.0`，同步更新配置、升级备份、永久防重和故障排查文档。

### Testing

- 先用回归测试复现旧结构缺少永久成员基线、老成员可因新的入群事件获得资格的问题，再完成修复。
- 使用本机 AstrBot `4.26.7`、Python `3.12.13` 执行 `pytest tests -q`：`22 passed`。
- 覆盖 SQLite v1 到 v2 迁移、当前群成员基线、启动同步完成前入群不授予资格、旧资格永久封锁、跨群全局防重、退群重进不新增资格、领取后永久封锁、同步失败停止发放和失败后兑换码回滚。
- 覆盖白名单群启动同步、空白名单全部群同步、基线刷新只新增不删除、Page 数据库人数、今日新人和 10 秒自动刷新契约。
- 对 Git 已跟踪 Python 文件执行 Ruff 检查和格式检查：全部通过。
- `py_compile main.py page_api.py storage.py`：通过。
- `node --check pages/gift_codes/app.js`：通过。
- `metadata.yaml`、`_conf_schema.json` 解析和 `v1.2.0` 配置契约：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示。
- 仓库根目录存在用户未跟踪文件 `single_account_breath_test.py`，直接执行 `pytest -q` 或 `ruff check .` 会收集该范围外文件并因其独立依赖/规则失败；本轮未修改、未删除、未提交该文件。
- 未连接真实 QQ/NapCat 执行在线成员基线与退群重进端到端测试；仍需更新插件后在测试群人工验收一次。

### Notes

- `main.py`：新增 AstrBot 初始化同步任务、OneBot 群/成员读取、运行期基线就绪保护和永久防重入群流程。
- `storage.py`：新增 schema v2、永久用户库、群成员基线、旧数据迁移、今日新人统计和成功领取资格清空。
- `pages/gift_codes/index.html`：新增数据库人数、今日新人卡片并更新发放规则。
- `pages/gift_codes/app.js`：读取新增统计字段并每 10 秒自动刷新。
- `pages/gift_codes/style.css`：将六张统计卡调整为桌面三列响应式布局。
- `_conf_schema.json`：新增可自定义的成员基线未完成提示，并更新无资格默认文案。
- `metadata.yaml`：版本升级为 `v1.2.0`。
- `README.md`：补充永久成员基线、防退群重进、升级备份和 Page 实时统计说明。
- `docs/USAGE.md`：补充 schema v2 迁移、永久防重规则、同步失败保护、统计口径和降级恢复要求。
- `tests/test_storage.py`：新增迁移、永久用户、基线刷新、退群重进与领取后封锁测试。
- `tests/test_plugin.py`：新增启动同步、空白名单全群同步和同步失败停止发放测试。
- `tests/test_page_api.py`：新增数据库人数和今日新人接口验证。
- `tests/test_static_contract.py`：更新版本、配置和 Page 自动刷新契约。
- `progress.md`：追加本轮实施、验证、范围外文件和回滚说明。
- 正式升级前应备份 `gifts.db`。回滚点为 `11a79c7`；代码可执行 `git revert <本次提交哈希>`，数据库若要完整降级必须恢复升级前备份。


## 2026-07-30 - Task: ?????????????

### What was done

- ??????????????????????? `lottery.db` ? `compensation.db`??? `gifts.db`???????????????????????
- ?? QuantumNous New API ????????????????????????????2FA ???HTTPS/???????? HTTP ?????????????
- ?? USD?CNY?CUSTOM?TOKENS ?????????? `/api/user/manage` ? `add_quota` + `mode=add` ???????
- ?????????/??/??/???????????? @ ????????????????????????????????????
- ??????????????QQ ? New API ID ???????????????????????/??/?????????????
- ???? `pending_confirmation`?`processing`?`paid`?`failed`?`manual_review`?`cancelled`?`expired` ??????????????/??/5xx ????????????? `processing` ????????
- ?? 5 ??????????/??/??????????????? UTC ????????????
- ??????? API ??????????????????????????? `*_content` ????????? `v1.3.0`?

### Testing

- ???? AstrBot `4.26.7`?Python `3.12.13` ?? `python -m pytest tests -q`?`42 passed`?
- ???? New API ??/?????2FA??????URL/TLS ??????????????????????????????
- ??????????????????????????????????????????????????????????????????????????
- ????? Page??????????????????????????????????
- `ruff check`????`ruff format --check`?`15 files already formatted`?
- `py_compile` ?????? Python ??????`node --check pages/gift_codes/app.js`????
- `metadata.yaml` ? `_conf_schema.json` ???????? `v1.3.0`???? 95 ??TLS ???????? HTTP ???????????????
- AstrBot 4.26.7 ???????????`newapi`?`??`?`??` ?????????? `CommandGroupFilter` ? `PermissionTypeFilter(ADMIN)`?
- ? QuantumNous New API ???????? `/api/status`?`/api/user/login`?`/api/user/{id}`?`/api/user/manage` ? `add_quota` ?????
- ??????? New API ????? NapCat ???????????????????????????????????????????????????????

### Notes

- `main.py`???????/????????????@????????5 ??????????????????
- `newapi_client.py`??? New API ???????????????????????????????
- `campaign_utils.py`??? UTC/???????/????????Decimal ?????????
- `campaign_messages.py`??????????? New API ??????????
- `lottery.py`???????????????????????????????????
- `compensation.py`?????????QQ/API ID ?????????????????????
- `_conf_schema.json`??? New API????????????/????????????
- `metadata.yaml`?????? `v1.3.0` ????????
- `requirements.txt`??? `httpx` ???
- `README.md`??? v1.3.0 ????????????????
- `docs/CAMPAIGNS.md`??? New API????????????????????????
- `docs/USAGE.md`?????/?????????
- `tests/test_newapi_client.py`??????????????????????????
- `tests/test_campaign_storage.py`?????/??????????????????
- `tests/test_plugin.py`??? AstrBot ??????????????????????????????
- `tests/test_static_contract.py`??? v1.3.0 ???/???????????????
- `progress.md`???????????????????????
- ?????????????????? Adobe2API ??????
- ?????? `main` ? `8c58af6`??????? `git revert <??????>`????????????????? AstrBot??????????????? `lottery.db` ? `compensation.db`?`gifts.db` ??????

## 2026-07-30 - Task: 更正抽奖与补偿系统实施记录

### What was done

- 上一条同日记录因终端编码问题显示为问号；本条按只追加规则提供完整更正内容，不改写既有历史。
- 新增与新人礼完全隔离的抽奖和补偿系统，分别使用 `lottery.db` 与 `compensation.db`，原有 `gifts.db`、新人资格、永久用户基线和兑换码逻辑保持不变。
- 新增 QuantumNous New API 兼容客户端，支持管理员访问令牌优先、用户名密码兼容登录、2FA 拒绝、HTTPS/证书校验、不安全 HTTP 显式开关和管理员权限测试。
- 新增 USD、CNY、CUSTOM、TOKENS 额度换算快照，并通过 `/api/user/manage` 的 `add_quota` + `mode=add` 原子增加额度。
- 完成抽奖草稿、时间/口令/描述/分级奖项配置、发布、精确 `@机器人` 报名、退群过滤、系统随机无重复开奖、确认领奖、截止作废、取消和人工核查。
- 完成补偿按时长或总预算开启、QQ 与 New API ID 双重去重、确认领取、最后一份预算并发保护、到时/预算/手动关闭和活动间重新计入。
- 统一实现 `pending_confirmation`、`processing`、`paid`、`failed`、`manual_review`、`cancelled`、`expired` 状态；明确失败释放占用，超时/断线/5xx 冻结且不自动重试，重启遗留 `processing` 自动转人工核查。
- 新增 5 秒调度器处理抽奖开始/开奖/领奖截止和补偿到期；所有时间以 UTC 入库、亚洲上海时区展示。
- 抽奖、补偿、New API 测试、管理员结果、用户确认和调度通知文案全部加入可配置 `*_content` 项；插件版本升级到 `v1.3.0`。

### Testing

- 使用本机 AstrBot `4.26.7`、Python `3.12.13` 执行 `python -m pytest tests -q`：`42 passed`。
- 测试覆盖 New API 令牌/密码认证、2FA、权限不足、URL/TLS 门禁、四种额度换算、原子加额请求体、明确失败与模糊结果分类。
- 测试覆盖抽奖发布、重复报名、退群过滤、开奖持久化、中奖确认、人工核查恢复，以及补偿双重去重、最后预算并发、明确失败释放预算、预算耗尽关闭和重启恢复。
- 新人礼原有 Page、永久成员基线、退群重进防刷、群临时会话失败回滚等回归测试全部通过。
- `ruff check`：通过；`ruff format --check`：`15 files already formatted`。
- `py_compile` 覆盖全部插件 Python 模块：通过；`node --check pages/gift_codes/app.js`：通过。
- `metadata.yaml` 与 `_conf_schema.json` 解析通过，版本为 `v1.3.0`，配置共 95 项；TLS 默认开启、不安全 HTTP 默认关闭、抽奖和补偿默认关闭。
- AstrBot 4.26.7 非侵入式加载验证通过：`newapi`、`抽奖`、`补偿` 三个命令组均同时注册 `CommandGroupFilter` 与 `PermissionTypeFilter(ADMIN)`。
- 对 QuantumNous New API 当前官方源码核对 `/api/status`、`/api/user/login`、`/api/user/{id}`、`/api/user/manage` 和 `add_quota` 请求契约。
- 未连接真实测试 New API 实例和在线 NapCat 群执行额度到账验收；发布后必须先在测试实例、测试用户和测试群完成一次真实加额，不得直接以生产用户作为首次测试。

### Notes

- `main.py`：接入独立开关/群白名单、管理员命令组、`@机器人` 用户流程、5 秒调度器、生命周期恢复和主动群通知。
- `newapi_client.py`：新增 New API 安全连接、认证、权限测试、用户查询、额度快照与原子加额客户端。
- `campaign_utils.py`：新增 UTC/上海时区、绝对/相对时间、时长、Decimal 和流水号公共工具。
- `campaign_messages.py`：集中维护抽奖、补偿和 New API 的全部默认聊天文案。
- `lottery.py`：新增抽奖数据库、业务状态机、退群过滤开奖、中奖确认、发放和人工核查。
- `compensation.py`：新增补偿数据库、QQ/API ID 去重、预算并发占用、发放、关闭和人工核查。
- `_conf_schema.json`：新增 New API、安全开关、两个系统开关/白名单和全部可配置文案。
- `metadata.yaml`：版本升级为 `v1.3.0` 并更新功能描述。
- `requirements.txt`：新增 `httpx` 依赖。
- `README.md`：补充 v1.3.0 功能、快速启用、风险和验证命令。
- `docs/CAMPAIGNS.md`：新增 New API、抽奖、补偿、失败核查、备份和回滚完整运维文档。
- `docs/USAGE.md`：增加抽奖/补偿独立文档入口。
- `tests/test_newapi_client.py`：新增认证、权限、额度换算、请求契约和错误分类测试。
- `tests/test_campaign_storage.py`：新增抽奖/补偿持久化、并发预算和重启恢复测试。
- `tests/test_plugin.py`：新增 AstrBot 用户路由、系统独立开关、退群开奖过滤和真实业务流程模拟测试。
- `tests/test_static_contract.py`：更新 v1.3.0 元数据/配置契约并验证管理员命令权限。
- `progress.md`：按只追加规则保留编码异常记录，并追加本条可读更正与完整实施证据。
- 本轮未修改、未删除、未暂存工作区现有 Adobe2API 未跟踪文件。
- 回滚点为当前 `main` 的 `8c58af6`；发布后可执行 `git revert <本轮提交哈希>`。如需同时移除新业务数据，应先停止 AstrBot、备份插件数据目录，再单独处理 `lottery.db` 与 `compensation.db`；`gifts.db` 无结构变更。
